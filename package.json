{
  "name": "fs-map-root",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build:sw && npm run build --workspace=backend && npm run build --workspace=frontend",
    "build:sw": "tsc -p frontend/tsconfig.sw.json",
    "start": "npm run start --workspace=backend",
    "test": "npm run test --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

