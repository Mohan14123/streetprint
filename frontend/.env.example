# Backend API base URL
# In development: leave empty — Vite proxy rewrites /api/* to the backend
# In production:  set to your deployed backend URL (e.g. https://api.streetprint.app)
VITE_API_URL=

# Mapbox public token (if using Mapbox — leave empty if using Leaflet only)
VITE_MAPBOX_TOKEN=
